language: python
python:
  - "2.7"
cache: pip
before_install:
#  - pip install pytest pytest-cov
#  - pip install coveralls
  - pip install -r test-requirements.txt
install:
  - pip install -r requirements.txt
script:
  - py.test
#  - python setup.py install && python -m pytest
after_success:
  - coveralls
branches:
  only:
    - master
